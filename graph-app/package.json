{
  "name": "nightshift-graph-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The /graph view, rebuilt on React Flow. The ONE sanctioned build-step surface in nightshift: its dist is committed to public/graph-flow/ so the zero-install clone keeps working, and the pure models (reducer/graph-model/digest) are loaded at RUNTIME from /public — never bundled.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xyflow/react": "^12.8.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^6.3.5"
  }
}
